# Python-Certificate-Automation

This project automates internship certificate generation using Python.

## Technologies Used
- Python
- Pandas
- DocxTemplate
- Excel

## How It Works
1. Reads intern data from Excel
2. Uses a Word template
3. Automatically generates certificates for each intern

## Use Case
Useful for companies and organizations generating large numbers of certificates.
